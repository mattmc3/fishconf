complete -x -c ipython -a profile -d "Create and manage IPython profiles."
complete -x -c ipython -a locate -d "Print the path to the IPython dir"
complete -x -c ipython -a kernel -d "Start a kernel without an attached frontend."
complete -x -c ipython -a history -d "Manage the IPython history database."
